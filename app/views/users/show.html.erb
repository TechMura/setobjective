<div class="contents">
  <div class="container"> 
    <div class="objective-lists">
      <% @objectives.each do |objective| %> 
        <div class="objective-list">
          <%= link_to "#{objective.declaration}", objective_path(objective.id) %>
        </div>
      <% end %>
    </div>
  </div>
</div>